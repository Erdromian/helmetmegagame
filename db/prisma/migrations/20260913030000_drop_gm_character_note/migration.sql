-- GmCharacterNote was orphaned by the /gm/dev desk rework: the notes-ledger
-- half of that feature was dropped upstream, so nothing has read or written
-- this table since. Verified empty (0 rows) before the drop.
DROP TABLE IF EXISTS "GmCharacterNote";
